
  # Restaurant Reservation Management App

  ## Running the code

  Run  
  ```powershell
         npm i
  ```
  to install the dependencies.

  Run
  ```powershell
         npm run dev
  ```
 
  to start the development server.
  
